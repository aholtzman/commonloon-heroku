import styled from 'styled-components'

const BG = styled.div`
  background: url(${require('../images/assets/bg2.jpg')});
  background-size: cover;
  width: 100vw;
  height: 100vh;
  overflow: scroll;
`

export default BG
